{# templates/partials/_topbar.html.twig #}
<div class="topbar-glass">
  <div class="container-fluid px-3 px-lg-4 py-2 d-flex justify-content-between align-items-center">
    <div class="small text-muted">Catalogue & export</div>
    <div class="small text-muted">
      {% if app.user %}Connecté : {{ app.user.email }}{% else %}Invité{% endif %}
    </div>
  </div>
</div>
